// Frontend runtime configuration, sourced from Vite env variables.
// See .env.example for the variables that can be overridden.
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL ?? "http://localhost:8000";
